find_replace:
  # Direct Lake: point model to the Lakehouse SQL endpoint (dynamic)
  - find_value: "u45polrydnzuhdejwn5hophdvm-5yahj72phemudpjjg2uuorfxe4.datawarehouse.fabric.microsoft.com"
    replace_value:
      PPE:  "$items.Lakehouse.bronze_lh.sqlendpoint"
      PROD: "$items.Lakehouse.bronze_lh.sqlendpoint"

  # Direct Lake: switch the model's database id (PATCHED by CI after pass #1)
  - find_value: "DL_DATABASE_ID_TO_BE_SET_BY_CI"
    replace_value:
      PPE:  "DL_DATABASE_ID_TO_BE_SET_BY_CI"
      PROD: "DL_DATABASE_ID_TO_BE_SET_BY_CI"
    item_type: "SemanticModel"

  # Your existing replacements (kept)
  - find_value: "2c3530fb-67bb-42e9-b4f3-9ced9e361e9c"
    replace_value:
      PPE:  "$items.SemanticModel.SemanticModel1.id"
      PROD: "$items.SemanticModel.SemanticModel1.id"

  - find_value: "ff7400ee-394f-4119-bd29-36a94744b727"
    replace_value:
      PPE:  "$workspace.id"
      PROD: "$workspace.id"

  - find_value: "47dc3d49-cfb5-419c-9e36-ab95a0421730"
    replace_value:
      PPE:  "$items.Lakehouse.bronze_lh.id"
      PROD: "$items.Lakehouse.bronze_lh.id"
